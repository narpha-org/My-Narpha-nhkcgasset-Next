query GetCGAssetUploadDirs($first: Int!, $page: Int!) {
  CGAssetUploadDirs(first: $first, page: $page) {
    data {
      id
      base_path
      order
      valid_flg
      created_at
    }
    paginatorInfo {
      count
      currentPage
      hasMorePages
      total
    }
  }
}
