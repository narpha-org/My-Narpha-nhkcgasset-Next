query GetCgAssetCates($first: Int!, $page: Int!) {
  CGAssetCates(first: $first, page: $page) {
    data {
      id
      code
      desc
      order
      valid_flg
      created_at
    }
    paginatorInfo {
      count
      currentPage
      hasMorePages
      total
    }
  }
}
