query GetCgaSharedAreas($first: Int!, $page: Int!) {
  CGASharedAreas(first: $first, page: $page) {
    data {
      id
      desc
      valid_flg
      created_at
    }
    paginatorInfo {
      count
      currentPage
      hasMorePages
      total
    }
  }
}
