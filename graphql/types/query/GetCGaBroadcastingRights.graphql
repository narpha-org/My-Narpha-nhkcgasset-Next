query GetCGABroadcastingRights($first: Int!, $page: Int!) {
  CGABroadcastingRights(first: $first, page: $page) {
    data {
      id
      desc
      valid_flg
      created_at
    }
    paginatorInfo {
      count
      currentPage
      hasMorePages
      total
    }
  }
}
