mutation UpdateCGASharedArea(
  $id: ID!
  $desc: String!
  $order: Int!
  $valid_flg: Boolean!
) {
  updateCGASharedArea(
    id: $id
    desc: $desc
    order: $order
    valid_flg: $valid_flg
  ) {
    __typename
    id
    desc
    order
    valid_flg
  }
}
