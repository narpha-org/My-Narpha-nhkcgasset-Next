mutation CreateCgaRegistrantAffiliation(
  $desc: String!
  $order: Int!
  $valid_flg: Boolean!
) {
  createCGARegistrantAffiliation(
    desc: $desc
    order: $order
    valid_flg: $valid_flg
  ) {
    __typename
    id
    desc
    order
    valid_flg
  }
}
